{
  "version": 2,
  "installCommand": "cd ../.. && npm ci --include=dev",
  "buildCommand": "cd ../.. && npm run build -w @autocore/database && npm run generate -w @autocore/database && npm run build -w @autocore/api && node apps/api/scripts/prepare-api-vercel.mjs",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 60,
      "includeFiles": "dist/**"
    },
    "api/ping.ts": {
      "maxDuration": 10
    },
    "api/env-check.ts": {
      "maxDuration": 10
    }
  }
}
